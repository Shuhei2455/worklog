// M0-a では接続確認だけ。全テーブルの定義は M0-b で行う
// (docs/02-roadmap.md「スキーマはこの段階で全部書く」)。
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
