# CLAUDE.md

このリポジトリで作業するときに最初に読むファイル。

## このプロジェクトは何か

Backlog（ヌーラボ）と同等の機能を持つ、自己ホスト型のプロジェクト管理アプリを作る。
利用者は職場チーム5〜20人。まず自宅サーバのDockerで開発・検証し、動いたら職場のVMへ移設する。

## 最初にやること

1. `docs/00-spec-verified.md` を読む。**これが仕様の正解データ**
2. `docs/01-design.md`（アーキテクチャとデータモデル）
3. `docs/02-roadmap.md`（マイルストーンと受け入れ条件）
4. `docs/PROGRESS.md`（どこまで終わっているか）

---

# 絶対に守る2つのルール

この2つは、他のどの指示よりも優先する。省略・後回し・まとめて実施のいずれも認めない。

## ルール1: 本家Backlogの仕様と比較するレビューを繰り返す

**各マイルストーンで最低2回、仕様レビューを行う。**

### 1回目 — 着手前レビュー

マイルストーンの実装を始める前に、必ず以下を実行する。

1. これから作る機能に関係する `docs/00-spec-verified.md` の章を読み直す
2. その章に書かれていない仕様が実装に必要かどうかを洗い出す
3. 必要なものがあれば**一次情報で確認する**
   - API: https://developer.nulab.com/docs/backlog/api/2/
   - ヘルプ: https://support-ja.backlog.com/
   - ネットワークが使えない場合は、確認すべき項目を列挙して**ユーザーに調査を依頼し、作業を止める**
4. 確認できた仕様は `docs/00-spec-verified.md` に追記する（CLAUDE.md には書かない）
5. 洗い出した結果を報告してから実装に入る

### 2回目 — 完了時レビュー

受け入れ条件を満たしたと判断したら、コードと本家仕様を突き合わせて**差分表**を作る。

| 項目 | 本家の仕様 | 実装 | 判定 |
|---|---|---|---|
| 優先度 | 3段階 id=2/3/4 | 同じ | 一致 |
| Backlog記法 | 独自記法あり | Markdownのみ | 意図的に相違（理由: 再現の実務価値なし） |
| （例）状態の並べ替え | Closedより後に置けない | 制約なし | **バグ → 修正必要** |

- 判定は「一致 / 意図的に相違 / バグ」の3値のみ。「おおむね一致」は使わない
- **バグが1件でもあればマイルストーンを閉じない。** 修正してレビューをやり直す
- 「意図的に相違」は理由を必ず書く。理由が書けないなら、それはバグ
- 差分表は後述のObsidianノートに必ず残す

### レビューで守ること

- 仕様を**憶測で埋めない**。似たツールの常識で判断しない
- 未確認の仕様に出くわしたら `TODO(要確認): <内容>` を残し、**作業を止めて報告する**
- 過去に実際、優先度を4段階（本当は3段階）、マイルストーンとバージョンを別テーブル（本当は同一）、ガントの表示条件を1パターン（本当は4パターン）と誤って設計した。すべて「常識的にはこうだろう」で埋めた箇所だった

## ルール2: マイルストーンごとにGitとObsidianへ記録する

**受け入れ条件を満たし、完了時レビューでバグが0件になったら、次のマイルストーンに進む前に必ず両方を実行する。** 片方だけで進めない。

### Git

```bash
git add -A
git commit -m "M<N>: <マイルストーン名>

受け入れ条件: すべて達成
仕様レビュー: バグ0件（意図的な相違 N件）
"
git tag -a m<N> -m "<マイルストーン名> 完了"
```

- タグは `m0` `m1` … の形式
- `docs/PROGRESS.md` に日付・所要セッション数・詰まった点を追記してからコミットする
- タグを打った後で追加修正が出たら、タグは打ち直さず次のコミットで対応する

### Obsidian

Vault の以下のパスにノートを作る。**パスはユーザーが指定した値を使う**（未設定なら聞く）。

```
<Vaultのパス>/obsidian/Projects/backlog-clone/M<N>-<マイルストーン名>.md
```

既存Vaultは `Projects/<プロジェクト名>.md` という構成で、`10_Projects/` という
フォルダは使っていない。Vaultを分断しないため既存の並びに合わせた
（ノート自体の構造＝下のテンプレートは変えていない）。
インデックスは `Projects/backlog-clone/backlog-clone-index.md` に置き、
Vault全体の入口である `Home.md` からもリンクする。

テンプレート（この構造を変えない）:

```markdown
---
project: backlog-clone
milestone: M<N>
date: YYYY-MM-DD
status: done
tags: [開発, backlog-clone]
---

# M<N> <マイルストーン名>

## やったこと
- （実装した機能を箇条書き。コミットの羅列ではなく、何ができるようになったかを書く）

## 本家仕様との差分レビュー

| 項目 | 本家の仕様 | 実装 | 判定 |
|---|---|---|---|
| | | | |

意図的に相違させた項目とその理由:
- （項目）: （理由）

## 新たに確認できた仕様
- （docs/00-spec-verified.md に追記した内容の要約）

## 詰まったこと
- （症状 / 原因 / 解決方法。同じことで次に詰まらないために書く）

## 次に判断が必要なこと
- （未確認の仕様、保留にした設計判断）

## リンク
- Gitタグ: `m<N>`
- [[backlog-clone-index]]
```

補足:
- Obsidian は素のMarkdownファイル群なので、ファイルを書けばそのまま反映される
- 初回は `backlog-clone-index.md` も作り、各マイルストーンノートへの `[[リンク]]` を並べる
- **Obsidianアプリを開いたままだと同期の競合が起きうる。** 書き込み前にユーザーに一声かける
- Vaultがリポジトリ外にある場合、書き込みが拒否されたらパスをユーザーに確認する

---

## その他のルール

### 見た目も本家に揃える（2026-09-13 に方針変更）

**変更前は「アプリ名・ロゴ・配色・アイコンは独自にする」だった。**
利用者が本家を使い慣れており、移行時の学習コストを下げる方が実務上重要なため、
ユーザーの判断で方針を変えた。

- **配色・余白・字の大きさ・画面の構成は本家に揃える。** 近づける努力をする
- 根拠は一次情報から取る（ルール1と同じ）。**公式APIドキュメントに載っている値を使う**。
  例: 状態の色 `#ed8077`（Get Status List）、状態に選べる10色（Add Status）
- ドキュメントに無い値は `TODO(要確認)` にして**ユーザーに確認する**。
  画面を見れば分かる値を憶測で埋めない（ルール1は有効なまま）
- **APIのパス・パラメータ名・レスポンス構造も本家に合わせる**（従来どおり）
- Backlog記法は実装しない。Markdown(GFM)のみ

揃えないもの:

- **アプリ名は `Kadai` のまま。** 本家の名前を名乗ると、どちらを触っているか
  分からなくなる（移設の検証で実際に困る）
- **ロゴ画像は本家のものを持ち込まない。** 画像ファイルの複製にあたり、
  かつ名前が違うので合わない

### データを壊さない

- スキーマ変更は必ず Prisma のマイグレーションファイルとして残す。`prisma db push` で済ませない
- 既存データを削除・変換するマイグレーションは、実行前に必ず確認を取る
- `docker compose down -v` を提案しない

## 技術スタック（変更しない）

| 層 | 採用 |
|---|---|
| フロント+API | Next.js (App Router) + TypeScript |
| DB | PostgreSQL 16 + Prisma |
| 非同期 | BullMQ + Redis |
| 検索 | Meilisearch |
| リアルタイム | Server-Sent Events（WebSocketは職場のプロキシで詰まる可能性がある） |
| Git | Gitea（Gitホスティングは自作しない） |
| UI | Tailwind + shadcn/ui + TanStack Table + dnd-kit |
| Markdown | remark / rehype（GFM） |
| メール | nodemailer（2026-09-12にユーザー承認のうえ追加。Nodeに標準のSMTPクライアントが無いため） |

別のライブラリを入れたい理由ができたら、勝手に入れずに提案する。

## コーディング規約

### 命名

- APIのフィールド名は**本家と同一**にする（`issueKey` `keyId` `summary` `assignee`）
- 本家の癖もそのまま踏襲する（`category` `milestone` が単数形キーで配列を返す、など）
- Prisma のモデル名は PascalCase、DBの物理名は snake_case（`@@map` / `@map`）
- UIの表示文字列は日本語

### 集約すべき関数（分散させない）

| 関数 | 役割 |
|---|---|
| `buildIssueWhere(filter)` | 課題フィルタからPrismaの `where` を組む。一覧・ボード・ガント・API・エクスポートが全部これを使う |
| `can(user, action, resource)` | 権限チェック。APIルートの入口で必ず通す |
| `resolveGanttBar(issue)` | 課題1件から `{ from, to, kind }` を返す純関数。表示条件4パターン |
| `describeChanges(changes)` | 変更差分JSONBを日本語ラベルに解決する |

### 権限

- 一覧クエリには**権限条件を注入する**。取得後にフィルタすると件数とページングが壊れる
- 管理者でも、参加していないプロジェクトの課題とWikiは見えない
- 「ユーザー種別 × 制限 × プロジェクト管理者フラグ」の3軸。単一のロール列挙にしない

### `"use server"` のファイル

**非同期関数しかエクスポートできない。** 定数も同期関数も置けない。
置くと `A "use server" file can only export async functions, found ...` で
ページ全体が 500 になる。エラーメッセージが原因を指さないので気づきにくい。

共有する定数や純関数は `src/lib/` に分ける。M0 と M3 で2回踏んだ。

### 開発コンテナで `pnpm build` を実行しない

`next dev` が使っている `/work/.next` を本番ビルドの成果物で上書きしてしまい、
開発サーバが `MODULE_NOT_FOUND` と `fallback-build-manifest.json が無い` で
500 を返すようになる。復旧は `.next` を消して app を再起動。

型チェックを含む本番ビルドの確認は、本番イメージをビルドすれば足りる
（ビルド段が別なので `.next` を汚さない）。

```bash
docker build -f docker/app.prod.Dockerfile -t backlog-clone-app:<タグ> .
```

### 採番

`keyId` は必ずトランザクション内で。アプリ側で MAX+1 を取らない。

```sql
UPDATE projects SET last_issue_no = last_issue_no + 1 WHERE id = $1 RETURNING last_issue_no;
```

## テスト

目視デバッグに向かないため、必ず単体テストを書く。

- `can()` の権限マトリクス
- `resolveGanttBar()` の4パターン
- `keyId` の並列採番（10件同時作成で重複しないこと）
- `buildIssueWhere()` の主要な条件組み合わせ

## 作らないもの

理由があって除外している。「あった方がよさそう」で追加しない。

- Gitホスティングの実装本体（Giteaに任せる）
- SVN対応
- コミットログのキーワード（`#fix` / `#close`）による状態変更 — **本家が2019年12月に廃止済み**
- ガントの依存関係と自動スケジューリング — 本家に無い
- Backlog記法
- Wikiのリアルタイム共同編集
- モバイルアプリ

## 環境変数

`.env.example` を正とする。新しい環境変数を足したら必ず追記する。外部SaaSに依存する設定を追加しない。
