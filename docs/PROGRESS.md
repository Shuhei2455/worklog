# 進捗

各マイルストーンの完了時に追記する。**追記してからコミットする**（CLAUDE.md ルール2）。

書くこと: 日付 / 所要セッション数 / 何ができるようになったか / 詰まった点。

---

## 2026-09-11 セットアップ（M0 着手前）

まだマイルストーンではない。着手前レビューと土台の準備。

### やったこと

- リポジトリを `~/docker/backlog-clone` に作成し、CLAUDE.md と docs 一式を配置
- 設計書が2つあったため、v1 を `docs/archive/backlog-clone-design-v1.md` へ退避し、
  冒頭に「破棄済み・参照禁止」と7箇所の誤りを明記した。
  v1 にしか無かったインデックス指定は `01-design.md` 4.3 へ移した
- `docs/00-spec-verified.md` を更新（10章を全面改稿、7章を訂正）

### 着手前レビューで判明した重要な誤り

**`roleType` の定義が設計書と食い違っていた。** 一次情報で確認した結果:

| roleType | 公式 |
|---|---|
| 1 | Administrator |
| 2 | Member, Guest (No restriction) |
| 3 | Member, Guest (Add issue only) |
| 4 | Member, Guest (View only) |

`01-design.md` の `role_type 3=ゲスト` は誤り。3は「課題の登録のみ」であり、
**ゲストは roleType では区別されない**。ゲスト判別用のカラムが別に要る（決定 D7）。

これは「常識的にはこうだろう」で埋めた典型で、放置すれば8件目の設計ミスになっていた。

### 一次情報で新たに確認できたもの

- カスタム属性の型 8種（typeId 1〜8）と型別の追加パラメータ
- 状態のレスポンス構造、状態追加時に選べる10色

### 決めたこと（本家仕様ではなく本アプリの決定。00-spec-verified.md 10.2 参照）

D1〜D10 の10件。特に M0 のスキーマに効くのは D6（マスタのidをプロジェクト内で1始まりにする）と D7。

### 次に判断が必要なこと

- `TODO(要確認)` が6件。いずれも M0 を止める性質のものではない

## 2026-09-12 M0-a / M0-b / M0-c

### M0-a 土台
`docker compose up` で7コンテナ(caddy/app/worker/postgres/redis/meilisearch/gitea)が
起動し、Caddy経由でアプリとGiteaに到達できる状態。

ポートは自宅サーバの既存サービス(Jellyfin 8096 / Home Assistant 8123 / VOICEVOX 50021)と
衝突しない値を選んだ。アプリの入口は 8088。

### M0-b スキーマ
44テーブルを定義しマイグレーション1本で適用。設計書4章に加え、
ロードマップが後段で要求する機能(検索条件の保存・webhook・監査ログ・
最近見た項目・カスタム属性)のテーブルも含めた。

**マスタのidは複合主キー方式(決定D6)。** 2プロジェクト作って両方のOpenが
id=1になることを確認済み。副次効果として、課題からの参照が
(projectId, statusId) の複合外部キーになり、別プロジェクトのマスタを
誤参照できなくなった。

Prismaで書けない制約13件はマイグレーションにCHECKとして手書きした。

### M0-c 認証と権限

**着手前レビューで権限マトリクスを一次情報から入手できた。**
出典: https://support-ja.backlog.com/hc/ja/articles/360035643434
`docs/00-spec-verified.md` 7.1 に全表を写した。実装に効いた発見:

1. **課題の削除は管理者とプロジェクト管理者だけ。** 制限なしの一般ユーザーでもできない
2. **「課題の登録のみ」は追加できるが編集できない。** 自分が作った課題も編集できない
3. **「課題の閲覧のみ」はコメントも付けられない**
4. **種別・カテゴリー・バージョンは「制限なし」の一般ユーザーでも編集できる。**
   プロジェクト管理者専用ではない
5. **共有ファイルは閲覧すら制限ユーザーに許されない。** 課題・Wikiと扱いが違う
6. 「追加権限」(契約管理者/プロジェクト管理者/メンバー招待/チーム管理者)という
   軸が設計書v2から抜けていた

`can()` は3軸のまま実装し、権限マトリクスを表のままテストデータに起こした。
131件のテストが通る。表が更新されたら配列を直すだけで漏れが落ちる。

### 詰まったこと

**Auth.js のリダイレクト先が http://0.0.0.0:3000 になり到達不能だった。**
Next を `-H 0.0.0.0` で起動しているため、Auth.js v5 が内部で作るURLが
バインドアドレスから組み立てられる。`AUTH_TRUST_HOST=true` も
`X-Forwarded-Host` の明示も効かなかった。

入口が LAN・Tailscale・職場VM と複数あり、設定でホスト名を固定したくないため、
Caddy の `header_down Location "^https?://0\.0\.0\.0:3000" ""` で
オリジンを剥がして相対パスにした。3経路すべてで正しく戻ることを確認済み。

**コンテナが root で動いており生成物が root 所有になっていた。**
マイグレーションやPrismaクライアントをホストから触れず、gitにも追加できない。
`user: "1000:1000"` で動かすよう変更した。

**pnpm 9 が依存のビルドスクリプトを既定で止める。**
`pnpm install` のたびにPrismaクライアントが消えるため、
`pnpm.onlyBuiltDependencies` と `postinstall: prisma generate` を追加した。

### 次に判断が必要なこと

- `TODO(要確認)` は7件。M0-d を止める性質のものは無い
- M0-d(プロジェクトCRUDとマスタ管理画面)で M0 の受け入れ条件が揃う

## 2026-09-12 M0 完了（土台）

所要: 1セッション（M0-a / M0-b / M0-c / M0-d の4段階に分けて実施）

### 何ができるようになったか

- `docker compose up -d` で7コンテナが起動し、`http://<host>:8088` でアプリが開く
- ログインできる（Auth.js / Credentials）
- プロジェクトを作れる。作成時に標準4状態と既定の課題種別4件が自動で入る
- プロジェクト設定画面で、機能のON/OFF・状態・種別・カテゴリー・
  バージョン・参加ユーザー・プロジェクト管理者フラグを編集できる
- 状態の並べ替え制約が画面で分かる。標準4状態はボタンが無効になり、
  ツールチップに「標準の4状態は並べ替えできません」と出る

### 受け入れ条件

| 条件 | 結果 |
|---|---|
| `docker compose up` だけで全コンテナが起動し、ログインしてプロジェクトを作れる | 達成 |
| 状態の並べ替えで Open より前・Closed より後に置けないことが UI で確認できる | 達成 |
| `pnpm test` で権限関数のテストが通る | 達成（148件） |

### 完了時レビュー

差分表は Obsidian の M0 ノートに記録。**バグ0件 / 意図的な相違4件**
（Backlog記法・状態の色・displayOrderの実値・使用中状態の置き換え先選択）。

### 詰まったこと（M0-d 分）

`"use server"` のファイルは非同期関数しかエクスポートできない。
定数を再エクスポートしていて 500 になった。エラーメッセージが
`found number` としか出ないので原因に気づきにくい。

### 次に判断が必要なこと

- `TODO(要確認)` は7件。M1 を止める性質のものは無い
- `TODO(M1)`: 使用中の状態を削除するとき、置き換え先を選ぶダイアログ
- M1 着手前レビューでは、課題のフィルタ条件と活動履歴まわりの仕様を洗う

## 2026-09-12 M1 完了（課題管理コア）

所要: 1セッション（M1-a / M1-b / M1-c / M1-d の4段階）

### 何ができるようになったか

- 課題の登録・編集・削除。課題キーは `PROJ-12` 形式
- 課題詳細でコメントと変更履歴が時系列に混ざって出る。
  **状態を変えつつコメントした操作は1件にまとまる**（本家と同じ）
- 添付ファイルのアップロード・ダウンロード（日本語ファイル名も可）
- 親子課題（1階層制約）と関連課題（両方向）
- 一覧の絞り込み・並び替え・ページング。条件はURLクエリなので貼れば再現できる
- 検索条件の保存
- ダッシュボード（自分が担当・自分が登録・最近見た課題・保存した条件）

### 受け入れ条件

| 条件 | 結果 |
|---|---|
| 10件同時作成で keyId が重複しない | 達成（結合テスト） |
| フィルタ条件を組み替えてもURLで共有でき、リロードで復元される | 達成 |
| 自分ひとりで実務のメモを課題として1週間運用できる | **未実施のまま閉じた**（ユーザー判断） |

3つ目は運用期間を要するため、形式的に満たしたものとして M1 を閉じた。
一覧の初期表示やソート順は決定D11（本家に記載なし）で決めており、
**実際に使ってみて違和感が出たら M2 の途中でも直す**。

### 完了時レビュー

差分表は Obsidian の M1 ノートに記録。**バグ2件を見つけて修正し、再レビューでバグ0件**。
意図的な相違4件。

見つけたバグ:
1. **フィルタの壊れた値1つで全条件が消えていた。** `?statusId=1&count=500` で
   `statusId` も `keyword` も失われる。絞り込んだURLを共有したとき、
   受け取った側で全件が出てしまう。zod の各項目に `.catch()` を付けて修正
2. **`sort` の6種が黙って無視されていた。** 受け付けるのに更新日順に落ちる。
   3種を `_count` で実装し、多対多の3種は理由を書いて意図的な相違に分類

### 詰まったこと

**添付の保存が EACCES で落ちた。** app/worker を `user: 1000:1000` で動かしている
一方、docker の名前付きボリュームは root 所有で初期化される。
`docker/fix-volume-perms.sh` を用意した。

**`"use server"` のファイルは非同期関数しかエクスポートできない。**
定数を再エクスポートして 500 になった。エラーが `found number` としか出ない。

**ブラウザでしか再現しない不具合を1件見落とした。**
Caddy の `{host}` がポートを含まないため、Next.js の Server Actions が
`Origin` と突き合わせて拒否し、ログインが 500 になっていた。
スクリプトからのHTTPでは `Origin` を送らないので検証を素通りしていた。
以後、画面に関わる変更はヘッドレスChromeで確認する。

### 次に判断が必要なこと

- `TODO(要確認)` は12件。M2 を止める性質のものは無い
- `TODO(M1)`: 使用中の状態を削除するときの置き換え先ダイアログ（M2以降に持ち越し）
- `TODO(M3)`: キーワード検索を Meilisearch の二段構えにする

## 2026-09-12 M2 完了（見せ方と通知）

所要: 1セッション（M2-a / M2-b / M2-c / M2-d の4段階）

### 何ができるようになったか

- **ガントチャート**。表示条件5通り＋非表示を `resolveGanttBar` に集約。
  絞り込み5つ・グルーピング5つ・タイムスケール4つ・
  「開始・期限日が未設定の課題」ダイアログ
- **ボード**。ドラッグ＆ドロップで状態変更、並び順の保存、
  4条件の絞り込み（単一選択）、検索条件の保存
- **SSEによるリアルタイム反映**。2つのブラウザでボードを開き、
  片方のドラッグがもう片方に反映されることを検証済み
- **通知の3経路＋担当**。お知らせ・担当・メンション・ウォッチを
  同一形式のレコードにまとめ、重複したら優先順で1件に寄せる
- ウォッチ（メモ付き）、スター（課題・コメント）、通知一覧

### 受け入れ条件

| 条件 | 結果 |
|---|---|
| ガントの表示条件すべてにテストがあり、通る | 達成（6分岐・20件） |
| 2つのブラウザでボードを開き、片方のドラッグが反映される | 達成 |
| 週次の進捗確認をこのアプリだけで済ませられる | **未実施のまま閉じた**（M1と同じ扱い） |

### 完了時レビュー

差分表は Obsidian の M2 ノートに記録。**抜け2件を見つけて修正し、再監査でバグ0件**。
意図的な相違5件。

見つけた抜け（どちらも「機能はあるのに画面から使えない」類）:
1. **ボードから検索条件を保存できなかった。** 一覧にはあるがボードに無かった
2. **コメントにスターを付けられなかった。** アクションはあるのにボタンが無かった

### 着手前レビューで見つかった仕様書の誤り（3件）

ここが今回いちばん効いた。

1. **ガントの表示条件**（5章）。「4パターン」と書いていたが5通り＋非表示の6分岐。
   さらに**マイルストーンと完了日は並列ではなく、マイルストーンが優先**。
   並列だと思って実装すると、終了日のあるマイルストーンを持つ完了課題が
   誤った位置に出る。ガントは v1 で1パターンと誤り、v2 でも2箇所ずれていた
2. **ガントの絞り込みは4つでなく5つ**（状態が抜けていた）。
   タイムスケールに四半期がある。サイドパネルで開始日・期限日も編集できる
3. **メンション記法**（3章）。`@ユーザー` ではなく
   `<@U{id}>` `<@T{id}>` `<@project>`。
   `@` で始まる普通の文字列はメンションにならない

### ボードで新たに分かった制約

- ボードは全プロジェクトで使える（ガントと違い設定のON/OFFが無い）
- **絞り込みは4つだけ・各条件は単一選択・状態では絞れない**
- カードの上限は無いが100件以上は件数表示が `+99`

### 詰まったこと

**SSE の購読者一覧を globalThis に置く必要があった。**
モジュール変数にすると Next の dev でホットリロードのたびに購読が迷子になる。

**自分の操作で返ってくるイベントで再取得すると画面が巻き戻る。**
ドラッグ直後はまだサーバーに反映されていないため。移動した課題IDを覚えて
自分のぶんは無視するようにした。

### 次に判断が必要なこと

- `TODO(要確認)` は14件。M3 を止める性質のものは無い
- app を複数インスタンスにするなら SSE の配信を Redis pub/sub に替える必要がある
- ガントのサイドパネル編集・ドラッグでの日付変更・短いURL は未実装（M3以降）
